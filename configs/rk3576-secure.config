# RK3576 secure-boot fragment —— EN 18031 SSM-2「验证启动」信任根。
#
# 叠加在 rk3576_defconfig 之上，打开 FIT 签名校验开关。底层 crypto 栈
# （HW crypto v2 / secure OTP / RSA / ARMv8 crypto）在 base defconfig 里已全开。
# make.sh rk3576-secure 自动用 keys/<hint> 签 uboot.img，把 RSA 公钥烤进 u-boot.dtb
# （验 boot.img）和 SPL dtb（验 uboot.img）。务必带 --spl-new（用刚编的、带 PSS 的 SPL）。
#
# 第三行的 PSS 开关必须有：我们的签名流程产 RSASSA-PSS 签名（os-next boot.its 与 bsp
# fit-resign 均用 pss），而 padding_pss_verify() 在 lib/rsa/rsa-verify.c 被该宏 ifdef 包住。
# 不开则 SPL/u-boot 编不进 PSS 验签代码 → 设备端验 PSS 镜像必失败（离线带 PSS 的
# fit_check_sign 会误报通过）。这是必要修复，但 **尚不充分**。
#
# ⚠️ 未竟（2026-06-25）：补上 PSS 后，secure u-boot 能跑到 bootcmd（rockusb 可枚举=init 正常），
#    但 boot_fit 在验证我们 ~53MB 外部数据(-E) boot.img 时**硬挂**——软件验签也挂、等 2 分钟
#    也不返回，非干净失败（失败会落 download）。疑 RK 验证启动对大 FIT 的内存布局/重叠。
#    需串口看 u-boot 控制台才能精确定位。当前 os-next 仍用 vendored 引导链（不验签）交付。
#    完整二分与证据见 os-next docs/uboot-bsp-vs-vendored-sb.md。
#
# 阶段化（见 bsp README / os-next 烧录 runbook）：
#   阶段 1（先验证）：签名镜像；公钥在 SPL/u-boot dtb 里。**未烧 eFuse 的板子照常启动**
#     （SoC 非安全态 → 不强制 eFuse 比对），可在样片上端到端验证整条签名链。
#   阶段 2（落锁，不可逆）：给 ./make.sh 传 --burn-key-hash，SPL 首启把公钥 hash
#     烧进 eFuse（需 CONFIG_SPL_FIT_HW_CRYPTO=y，base 已开）。务必先样片验证再上量产。
#
# 构建：./make.sh rk3576-secure CROSS_COMPILE=<prefix>
CONFIG_BASE_DEFCONFIG="rk3576_defconfig"
CONFIG_FIT_SIGNATURE=y
CONFIG_SPL_FIT_SIGNATURE=y
CONFIG_FIT_ENABLE_RSASSA_PSS_SUPPORT=y
